{
  "name": "gymflow-firestore-rules-tests",
  "private": true,
  "type": "module",
  "description": "Test delle regole Firestore (US-087) contro l'emulatore locale. Non fa parte dell'app Flutter: infrastruttura di test separata, non tocca pubspec.yaml.",
  "scripts": {
    "test": "cd .. && firebase emulators:exec --only firestore --project demo-gymflow-test \"node --test firestore-tests/rules.test.mjs\"",
    "verify-deploy": "node verify-deploy.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.2",
    "firebase-tools": "^13.29.1"
  }
}
